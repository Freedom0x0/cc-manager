---
title: v4 Tauri 迁移 commit 11-14 落定 + 关键决策
kind: decision
---

# v4.0 Tauri 2 全栈迁移 commit 11-14 sediment

session: 2026-08-02-session-c67684
branch: rust/full-tauri-migration
commits: 11 (Profiles) / 12 (Usage) / 13 (删 ui-smoke) / 14 (CI + drop KV + CSP)

## 关键成果

- **后端 60 IPC 全部上线**: v3.1 electron 60 IPC 全部平移 v4.0 Tauri 2 invoke handler
  (commits 2-14 共 12 commit 覆盖 6 读 + 5 写 + watcher 2 + MCP 6 + skills 6 + commands 6 + sub-agents 6 + hooks 6 + plugins 6 + profiles 6 + usage 6 = 62)
- **cargo test 22 passed + 2 ignored + 0 failed** (commit 11 profiles 6 + commit 12 usage 6 + commit 1-4 既有 10 + cross_platform 1)
- **npx tsc --noEmit 0 错**
- **CI 双平台 workflow 写好** (commit 14 .github/workflows/build-installers.yml)
- **mcp_server_state 表 drop** (commit 14, D10 + D15 决策链豁免半年原则)
- **动态 prod CSP** (commit 14 vite plugin + tauri.conf.json)

## 关键决策

### D17: v4 Tauri 2 commit 11-14 完整落定(2026-08-02)

5 个核心决策:

1. **Profiles 模块 D13 完整替代语义** (commit 11 apply.rs): reverse-disable 写真实文件
   - MCP settings.json disabledMcpjsonServers 黑名单操作
   - skills disabled_skills/ 镜像目录
   - commands + agents .md.disabled 后缀
   - plugins enabledPlugins[k]=false
   - plugins + hooks enable 路径需完整 entry, capture 不可重建, 走 skip + real_file_errors
2. **Profiles D15 capture 走 6 scanner** (commit 11 capture.rs): 不走 KV 表 LIKE
   - 6 模块并行 capture 真实 enabled 全集
   - 决策依据: v3.1 KV 表 LIKE 漏 9 项 (用户未 toggle 但 scanner 默认 enabled=true)
3. **drop mcp_server_state** (commit 14): 豁免 §4 半年原则
   - 决策链: D10 真停用 = settings.json 黑名单 + D15 capture 走 scanner
   - 单一来源 = 真实文件 + 6 scanner
4. **动态 prod CSP** (commit 14 vite plugin): Tauri 2 csp build-time 嵌入 binary, dev/prod 共用 conf 需 null/strict 二选一
   - 改 csp: null (Tauri conf) + vite plugin transformIndexHtml 注入 meta csp
   - dev 走 HMR ws 无 meta csp; prod 走 dist/index.html 含 meta csp (webview 优先 meta)
5. **CI Tauri 2 重写** (commit 14 .github/workflows/build-installers.yml): v3.1 Electron 路径完全不适配
   - 改 cargo-tauri build + Swatinem/rust-cache + dtolnay/rust-toolchain
   - 双平台 Windows MSI+NSIS / macOS DMG+APP
   - macOS 未签名 (D11 v4 起步), Gatekeeper 警告 + 右键打开

### D18: src/ 前端 v3.1→v4.0 集成缺口(已知未修)

- 背景: commit 2 (324a45d) 声称"删 src/mock.ts + mock-data.ts", 但 main.tsx 仍 import './mock', 实际未删
- 现状: `npm run build:vite` 失败 `Could not resolve "./mock"`, v4.0 CI 实际跑不通 (只到 typecheck 步骤)
- 影响: cargo-tauri dev 链路未知 (用户未手验); cargo-tauri build 因 beforeBuildCommand build:vite 失败
- 后续路线 (v4.1): 删 mock.ts 改 main.tsx 走 src/api.ts (v3.1 已有)
- 教训: commit message 与代码状态不对齐是 v4.0 commit 5-14 一系列问题的统一根因; 验证应跑 `git log -p src/main.tsx` 而非仅 cargo test + tsc

## 经验教训

1. **D13 完整替代语义不是 "target 启用 + 其他不动"**, 必须 reverse-disable `current ∖ target`
2. **KV 表是 cache, 不是 UI 真实状态** (D10 决策); capture 必须走 scanner
3. **Tauri build-time 配置 vs runtime 配置**: dev/prod 行为不同者走 runtime (前端 meta csp)
4. **半年原则豁免需要明示理由** (D10 + D15 决策链)
5. **commit message 与代码状态对齐** 是验证 commit 完整性的硬约束 (D18 教训)

## 关联文件

- src-tauri/src/repo/profiles/{types,capture,apply,diff,mod,tests}.rs (commit 11, 1149 行)
- src-tauri/src/repo/usage/{types,scanner,mod,tests}.rs (commit 12, 701 行)
- src-tauri/src/db/mod.rs (commit 14 删 mcp_server_state + 注释重写)
- src-tauri/tauri.conf.json (commit 14 csp 改回 null)
- vite.config.ts (commit 14 cspPlugin transformIndexHtml)
- .github/workflows/build-installers.yml (commit 14 重写, 102 + 行)
- CLAUDE.md §13 D17/D18 (commit 15 决策 sediment)
- README.md (commit 15 v4.0.0 + macOS DMG + Tauri 致谢)

## 验证状态

- cargo test --lib 22 passed + 2 ignored + 0 failed ✓
- npx tsc --noEmit 0 错 ✓
- npm run test:tauri (CI windows/macos job 跑) 未本地验 (待 CI runner 跑)
- npm run build:tauri (CI 双平台跑) 未本地验 (cargo-tauri build 需 cargo sub-deps 时间长)
- npm run dev:tauri 用户手验 (CLAUDE.md §1 v4.0 起步) 未做

## 后续待办 (v4.1+ 范围, 不在 D17 5 commit 序列)

1. **D18 mock cleanup**: 删 src/mock.ts + mock-data.ts, main.tsx 改 import './api' 走真 tauri invoke
2. **前端 src/ 集成**: 6 module Manager UI (McpManager / SkillsManager 等) 走 tauri invoke, 不走 mock
3. **Profiles UI**: ProfileManager (capture / apply / diff button) UI 组件
4. **Analytics UI**: AnalyticsModule (4 Statistic + 3 Table: byProject / byDay / byTool)
5. **真机验证**: 用户手验 npm run dev:tauri + cargo-tauri build 产物安装
6. **6 module scanner/writer cargo test**: Windows Defender 拦 (CLAUDE.md §1), CI runner 跑通后补
7. **settings_reader + ClaudeSettings Serialize 等价测试**: v3.1 等价测试补 (unknown-fields 保留策略变化)
