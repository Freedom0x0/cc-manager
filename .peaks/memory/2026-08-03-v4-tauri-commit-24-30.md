---
title: v4 Tauri 迁移 commit 24-30 (D26-D31 + 最终收尾)
kind: decision
---

# v4.0 Tauri 2 commit 24-30 — 11 commit + 6 决策 sediment

session: 2026-08-02-session-c67684 (2026-08-02 ~ 2026-08-03)
branch: rust/full-tauri-migration
commits: 24 (D26 importer) / 25 (D27 window.api) / 26 (D28 description) / 27 (D29 capture) / 28 (D30 plugin schema) / 29 (D31 plugin path) / 30 (本 commit 收尾)

## v4.0 30 commit 序列最终进度

commit 24-29 共 6 个 fix/feat + commit 30 收尾 = **commit 30 / 30 = 100% 完成**。

## 6 关键决策 (D26-D31)

### D26 (commit 24) — watcher + importer 集成

v4.0 commit 4 (4e4b99c) 平移 notify watcher 但只把事件写 KV 表, **importer 解析 + 启动触发** 漏。`cmd_watcher_rescan_all` stub, lib.rs setup 只建 schema。修复: 新增 `src-tauri/src/importer.rs` 270 行 (scan + parse + import_project_folder + rescan_all), setup 启动自动 rescan, cmd_watcher_rescan_all 真做事, 前端按钮 + handleRescan。

### D27 (commit 25) — v3.1 window.api 残留

commit 24 (D26) WatcherStatusIndicator 按钮用 `window.api.watcherRescanAll()`, v4 Tauri 2 不走 window.api (v3.1 Electron preload inject 模式 v4 已无), undefined 触发 TypeError。修复: WatcherStatusIndicator 改 `import { api }`, `global.d.ts` 130 行 → 6 行 v3.1 残留 stub。

### D28 (commit 26) — Profile 6 模块 description + UI 删 D13 文案

v3.1 → v4 ProfileModuleItem 平移丢 description 字段。修复: ProfileModuleItem + 5 module 类型加 description, `src-tauri/src/repo/common.rs` 65 行 parse_frontmatter_description helper (4 test), 3 scanner 解析 frontmatter, ProfileManager UI 详情/diff 列表渲染 `<name> — <description>` + 删 2 处 D13 文案。

### D29 (commit 27) — Profile capture 改 production 路径

`cmd_profile_create` 错用 `CaptureOptions::from_base_dir(&home_dir())`, from_base_dir 是 test fixture 工厂 (期望 ~/mcp.json), 生产实际 ~/.claude.json。修复: 3 handler (create/apply/diff) 改 `::default()`, scanner 内部 `default_*_dir()` 自动走生产路径。

### D30 (commit 28) — plugins_scanner v2 schema

`installed_plugins.json` 实际 v2 schema (`{"version": 2, "plugins": {"name@marketplace": [data]}}`), v4 commit 10 按 v1 平铺解析 → 0 plugin。修复: read_installed_plugins 改读 v2 (拿 plugins 嵌套 + value array.first), InstalledPlugin 加 version 字段。

### D31 (commit 29) — plugins_scanner 接 opts + installPath 路径

commit 28 修 schema 后仍 0 plugin, 暴露 3 二次 bug: (1) `_plugins_root` 带 _ 前缀忽略 opts; (2) plugin.json 路径用 root-based 错 (应是 v2 cache installPath); (3) read_installed_plugins 硬编路径同样 fixture 隔离陷阱。修复: 去 _ 前缀, 加 installed_plugins_path 参数, plugin.json 路径优先 installPath 走 v2, fallback root 走 v1。

## 关联文件

- src-tauri/src/importer.rs (D26 新增 270 行)
- src-tauri/src/repo/common.rs (D28 新增 65 行)
- src-tauri/src/repo/plugins_scanner.rs (D30/D31 改)
- src-tauri/src/lib.rs (D26/D27/D29/D31 改)
- src/components/WatcherStatusIndicator.tsx (D26/D27 改)
- src/global.d.ts (D27 改)
- src/modules/profiles/ProfileManager.tsx (D28 改)
- src/types.ts + src-tauri/src/types.rs (D28 改)
- src-tauri/src/repo/profiles/types.rs + capture.rs + diff.rs (D28 改)
- src-tauri/src/repo/skills_scanner.rs + commands_scanner.rs + agents_scanner.rs (D28 改)
- src-tauri/src/repo/mcp_scanner.rs (D28 改)

## 验证状态

- cargo test --lib 36 passed + 0 failed + 2 ignored ✓
- npx tsc --noEmit 0 错 ✓
- npm run build:vite 15.90s 成功 ✓
- cargo check 0 错 ✓
- 真机手验 (用户必跑, commit 30 后):
  - npm run dev:tauri 启
  - 9 panel 列表 (sessions / mcp / skills / commands / sub_agents / hooks / plugins / profiles / usage) 看数据
  - 点 Header "立即扫描" → 立即导入
  - Profile Tab → 捕获 → 看 6 模块 enabled 全集 (含 description)
  - Profile apply / diff 路径
  - Plugins Tab → 列表 1+ 项 (按 ~/.claude/plugins/installed_plugins.json 解析)

## 6 教训 (D26-D31)

1. **"集成" commit 必须 3 段全跑通** (D26) — notify 事件 + importer 解析 + 启动触发, 缺 1/3 = "集成" 名不副实
2. **复制 v3.1 代码必须验 v4 路径** (D27) — `window.api.X` v3.1 静默 undefined, tsc 不抓
3. **UI 文案只讲副作用** (D28) — 实现细节 (D13) 写 commit msg / CLAUDE.md sediment, 不进用户面
4. **fixture 工厂 vs production 工厂命名分离** (D29) — from_base_dir + Default 不能混用
5. **v3.1 → v4 schema 平移看真实数据** (D30) — 不能凭注释, 实际数据 v2 升了
6. **fixture 测试 + 真机手验 = 4 层验证** (D31) — cargo test case1/2 fail (fixture 隔离) + 真机手验 plugin 0 同时指明同一 bug

## v4.1 follow-up (用户 push 后开新会话)

1. 推送 tag `v4.0.0-rust-migration` → CI runner 出 MSI / NSIS / DMG / APP
2. plugins_scanner 加 v2 test fixture (commit 28/29 缺, 留 v4.1)
3. 6 module scanner/writer 完整 fixture 测试 (Windows Defender 拦, CI 跑通后补)
4. settings_reader + ClaudeSettings v3.1 等价测试
5. macOS 真机验证 + Apple Developer ID + notarization
6. 4-10 个 plugins/hooks enable 路径补 (v4 commit 11 apply.rs skip 项)

## 净增

commit 24-30 共 +819 / -29 / 17 文件 (含 CLAUDE.md / memory sediment / importer.rs / common.rs 4 新文件 + lib.rs / 6 scanner / 3 handler / 前端 UI 等 13 修改)。